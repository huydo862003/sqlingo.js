import type { Expression } from './expressions';

export class Plan {
  // Placeholder
}

export function plan (expression: Expression): Plan {
  throw new Error(`plan not implemented: ${expression}`);
}
